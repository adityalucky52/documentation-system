/**
 * Templates Feature — Stub (Client)
 *
 * This module is a placeholder for the documentation template gallery.
 *
 * Architecture Rules (from user):
 * - Templates must be stored independently. Never reference template data directly inside user spaces.
 * - Always clone templates into user-owned records before the editor opens.
 * - The editor NEVER creates spaces — it only receives a Space and renders it.
 *
 * Planned functionality:
 * - Template gallery UI for browsing available Prisma Docs templates.
 * - Template preview modal.
 * - Template clone API call that creates a new Space from the template content.
 * - Integration with the space-creation wizard (SpaceOnboardingOptions).
 */
export {}
